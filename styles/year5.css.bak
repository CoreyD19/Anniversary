@import url('base.css');
@import url('year1.css');
@import url('year2.css');
@import url('year3.css');
@import url('year4.css');

.puzzle-section {
  background-color: #292929;
  padding: 2rem;
  border-radius: 10px;
  box-shadow: 0 4px 12px rgba(255, 110, 199, 0.2);
  margin-bottom: 2rem;
}

.puzzle-section h2 {
  text-align: center;
  color: #ffb6e6;
  margin-bottom: 1rem;
}

.puzzle-section p {
  text-align: center;
  margin-bottom: 1.5rem;
  font-size: 1rem;
  color: #ffcce6;
}

#generateWordSearchBtn {
  padding: 0.6rem 1.2rem;
  background-color: #e38ea0;
  border: none;
  color: white;
  font-size: 1rem;
  border-radius: 8px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

#generateWordSearchBtn:hover {
  background-color: #d46c8b;
}

.puzzle-grid {
  display: grid;
  /* Default for desktop: 12 columns, each 35px */
  grid-template-columns: repeat(12, 35px);
  gap: 4px;
  justify-content: center;
  margin: 1rem auto;
  /* Ensure overflow is hidden by default on desktop, becomes auto on mobile */
  overflow-x: hidden;
}

.puzzle-grid div {
  width: 35px;
  height: 35px;
  background-color: #1e1e1e;
  color: #ffcce6;
  border: 1px solid #ffb6e6;
  font-size: 1.1rem;
  font-weight: bold;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  box-sizing: border-box;
  user-select: none;
}

.puzzle-grid .found {
  background-color: #a4dcbe;
  color: #ffffff;
}

#word-list-display {
  margin-top: 1rem;
  font-size: 1rem;
  font-style: italic;
  color: #4b2e83;
}
.word-to-find {
  display: inline-block;
  margin: 0 6px;
  font-weight: bold;
  color: #ffcce6;
}

.tile.selected {
  outline: 2px solid #ffd700;
  background-color: #444;
}

.tile.found {
  background-color: #a4dcbe;
  color: #fff;
}
.confetti {
  position: fixed;
  top: -10px;
  font-size: 20px;
  animation: fall 3s linear forwards;
  z-index: 9999;
}

@keyframes fall {
  to {
    transform: translateY(100vh) rotate(360deg);
    opacity: 0;
  }
}


/* ---------------------------------------------------- */
/* RESPONSIVE MOBILE ADJUSTMENTS              */
/* ---------------------------------------------------- */

@media (max-width: 768px) {
  .puzzle-grid {
    /* Use a CSS variable for the number of columns, provided by JS.
       Each column takes up an equal fraction of space. */
    grid-template-columns: repeat(var(--grid-cols-mobile, 12), 1fr);
    gap: 1px; /* Very small gap on mobile */
    justify-content: start; /* Align to start for horizontal scrolling */
    padding-bottom: 0.5rem; /* Add some bottom padding if it scrolls */
    overflow-x: auto; /* Allow horizontal scrolling if content overflows */
    -webkit-overflow-scrolling: touch; /* Smoother scrolling on iOS */
  }

  .puzzle-grid div {
    /* Let the grid cells size themselves */
    width: auto;
    height: auto;
    /* Explicit min/max height based on aspect ratio, adjusted to font */
    min-width: 25px; /* Minimum width for readability */
    min-height: 25px; /* Minimum height to match */
    padding: 2px; /* Small internal padding for letters */
    font-size: 0.8rem; /* Smaller font for mobile */
    line-height: 1; /* Ensure text fits vertically */
  }

  /* Adjustments for the puzzle section padding on smaller screens */
  .puzzle-section {
    padding: 1rem;
    margin-left: 0.5rem; /* Small left/right margin to prevent content touching edges */
    margin-right: 0.5rem;
    box-shadow: none; /* Reduce heavy shadows on small screens for performance/simplicity */
  }

  /* Smaller button on mobile */
  #generateWordSearchBtn {
    padding: 0.4rem 0.8rem;
    font-size: 0.85rem;
  }

  #word-list-display {
    font-size: 0.9rem;
    text-align: center;
    padding: 0 0.5rem; /* Ensure words don't overflow */
  }
}

/* Specific adjustments for very small screens (e.g., iPhone SE) */
@media (max-width: 400px) {
  .puzzle-grid div {
    min-width: 22px;
    min-height: 22px;
    font-size: 0.75rem;
  }
}
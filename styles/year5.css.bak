@import url('base.css');
@import url('year1.css');
@import url('year2.css');
@import url('year3.css');
@import url('year4.css');

.puzzle-section {
  background-color: #292929;
  padding: 2rem;
  border-radius: 10px;
  box-shadow: 0 4px 12px rgba(255, 110, 199, 0.2);
  margin-bottom: 2rem;
}

.puzzle-section h2 {
  text-align: center;
  color: #ffb6e6;
  margin-bottom: 1rem;
}

.puzzle-section p {
  text-align: center;
  margin-bottom: 1.5rem;
  font-size: 1rem;
  color: #ffcce6;
}

#generateWordSearchBtn {
  padding: 0.6rem 1.2rem;
  background-color: #e38ea0;
  border: none;
  color: white;
  font-size: 1rem;
  border-radius: 8px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

#generateWordSearchBtn:hover {
  background-color: #d46c8b;
}

.puzzle-grid {
  display: grid;
  /* Default for desktop: 12 columns, each 35px */
  grid-template-columns: repeat(12, 35px);
  gap: 4px;
  justify-content: center;
  margin: 1rem auto;
  overflow-x: hidden; /* Default to hidden, will be auto for mobile */
}

.puzzle-grid div {
  width: 35px;
  height: 35px;
  background-color: #1e1e1e;
  color: #ffcce6;
  border: 1px solid #ffb6e6;
  font-size: 1.1rem;
  font-weight: bold;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  box-sizing: border-box;
  user-select: none;
}

.puzzle-grid .found {
  background-color: #a4dcbe;
  color: #ffffff;
}

#word-list-display {
  margin-top: 1rem;
  font-size: 1rem;
  font-style: italic;
  color: #4b2e83;
}
.word-to-find {
  display: inline-block;
  margin: 0 6px;
  font-weight: bold;
  color: #ffcce6;
}

.tile.selected {
  outline: 2px solid #ffd700;
  background-color: #444;
}

.tile.found {
  background-color: #a4dcbe;
  color: #fff;
}
.confetti {
  position: fixed;
  top: -10px;
  font-size: 20px;
  animation: fall 3s linear forwards;
  z-index: 9999;
}

@keyframes fall {
  to {
    transform: translateY(100vh) rotate(360deg);
    opacity: 0;
  }
}


/* ---------------------------------------------------- */
/* RESPONSIVE MOBILE ADJUSTMENTS              */
/* ---------------------------------------------------- */

@media (max-width: 768px) {
  .puzzle-grid {
    grid-template-columns: repeat(var(--grid-cols-mobile, 12), 1fr);
    gap: 2px; /* Slightly increase gap for better visual separation */
    justify-content: start;
    padding-bottom: 0.5rem;
    overflow-x: auto; /* Allow horizontal scrolling if content overflows */
    -webkit-overflow-scrolling: touch; /* Smoother scrolling on iOS */
    /* Add some horizontal padding to the grid itself to prevent letters from touching screen edge */
    padding-left: 5px;
    padding-right: 5px;
  }

  .puzzle-grid div {
    /* Use padding-top for aspect ratio control */
    width: 100%; /* Take full width of its grid cell */
    height: 0; /* Important for padding-top hack */
    padding-top: 100%; /* Makes height equal to width (1:1 aspect ratio) */
    position: relative; /* For positioning content inside */

    /* Ensure content is centered within the new aspect-ratio box */
    display: flex;
    align-items: center;
    justify-content: center;

    min-width: 28px; /* Slightly larger min-width to ensure readability, adjust as needed */
    min-height: 28px; /* Should ideally be controlled by padding-top, but good as a fallback */
    font-size: 0.9rem; /* Adjust font size, try 0.9rem or 0.85rem */
    
    border: 1px solid #ffb6e6; /* Ensure border is still there */
    box-sizing: border-box; /* Crucial to include padding/border in the total size */
  }

  /* Target the text content inside the div to ensure it centers */
  .puzzle-grid div span { /* You might need to wrap the letter in a span if you want to explicitly style it */
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
  }

  /* Adjustments for the puzzle section padding on smaller screens */
  .puzzle-section {
    padding: 1rem 0.5rem; /* Reduced horizontal padding */
    margin-left: 0; /* Remove fixed margins, let padding handle it */
    margin-right: 0;
    box-shadow: none;
  }

  /* Smaller button on mobile */
  #generateWordSearchBtn {
    padding: 0.4rem 0.8rem;
    font-size: 0.85rem;
  }

  #word-list-display {
    font-size: 0.9rem;
    text-align: center;
    padding: 0 0.5rem;
  }
}

/* Specific adjustments for very small screens (e.g., iPhone SE) */
@media (max-width: 400px) {
  .puzzle-grid div {
    min-width: 25px; /* Even smaller min-width for very narrow screens */
    min-height: 25px;
    font-size: 0.8rem; /* Slightly smaller font */
  }
}